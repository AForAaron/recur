# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是作者本人（单一用户，非多租户）。使用情境：手上有十余个分散在不同平台的
订阅，横跨人民币与澳元两种主要结算币种，其中一部分是自动续费、一部分需要手动续费、
一部分是限期试用。这些订阅分散在腾讯云、OpenAI、Anthropic、淘宝、运营商等互不相通
的平台后台，没有统一的查看入口。

要完成的任务：知道「什么时候会被扣钱、扣多少」，并在扣费前决定是否继续。
次要任务：回顾「过去花了多少」，判断哪些订阅值得留下。

未来可能扩展到其他用户，但当前不做多租户设计。

## Product Purpose

记录并追踪周期性付费订阅的续费时点与金额，把分散在各平台的扣费信息集中到一处，
让用户在钱被划走之前就看见它。

存在的理由：自动续费的默认状态是「静默扣款」，用户往往在账单出现后才发现，
此时已经错过取消窗口。

成功的定义：
- 不再出现「不记得订过、却被扣了钱」的情况；
- 每个续费决策（继续 / 取消）都发生在扣费之前，而不是之后。

## Positioning

三个相邻产品难以同时复制的机制：

1. **试用与正式订阅在四个层面完全隔离**——数据、列表、提醒、统计。市面上的记账工具
   通常把免费试用和付费订阅混在同一列表里，导致「年度支出」被未付费项目污染。
2. **多币种强制归一**——所有订阅折算到人民币做聚合统计，同时保留原币种作为主显示，
   使「A$30/月」和「¥39/月」可以在同一维度比较。
3. **刻意不接入账单**——不解析邮件、不读银行流水、不 OCR 截图。数据完全由用户手动录入，
   代价是录入成本，换来的是零授权、零隐私暴露、零平台依赖。

## Operating Context

- 当前主要在浏览器（H5）中使用，手机浏览器与桌面浏览器都可能打开。
- 数据存于浏览器本地存储（localStorage），无账号、无云端、无同步。
- 添加订阅时手动填写：名称、金额、币种、周期、首次扣费日、是否自动续费、是否试用。
- 内置常见订阅模板（Netflix、ChatGPT、Claude、Cursor 等），可一键预填。
- 提醒通过浏览器通知（Notification API）送达，仅在页面打开时生效。
- 用户在澳大利亚与中国两地都有订阅，因此同时存在 AUD 与 CNY 结算。

## Capabilities and Constraints

**已实现**
- 订阅的增删改查，含试用开关与试用结束后行为（固定为「提醒我手动决定」）
- 自动滚动下次扣费日
- 四个视图：全部 / 正式 / 试用 / 非自动续费
- 六种排序：下次扣费日、月均价格（升降）、累计已付、名称、添加时间
- 日均成本、月均、年均、累计已付（简化公式）
- 分类饼图、未来 12 个月支出预测柱图
- 10 种币种 + 可编辑汇率表
- 浏览器通知、数据导出 JSON、PWA manifest

**约束**
- 数据来源必须是手动录入，不引入邮件解析、银行账单、OCR
- 无账号系统；如需云端同步，须先解决单用户密钥方案
- 累计已付当前用简化公式，对暂停/试用场景有误差，精确版需引入扣费流水表
- 浏览器通知无法在页面关闭时触发，可靠提醒需云函数定时器

**已决定但不做**
- 不做自动取消订阅（风险不可控，只提供退订链接跳转）
- 不做订阅推荐、社区排行

**未决**
- 是否接入云端同步（CloudBase / Supabase / 不接入）
- 微信小程序订阅消息需要服务号资质，尚未确认是否具备
- 深色模式在类型定义中已预留（`Theme = auto | light | dark`），尚未实现

## Brand Commitments

- **名称**：Recur。中文副名未定。
- **语气**：克制、直接、不推销。工具属性强于社区属性。
- **设计语言**：已固化在 `src/uni.scss` 与项目 `CLAUDE.md`——
  主色 `#6366F1`（柔和紫蓝），页面底色 `#F9FAFB`，卡片纯白圆角 `20rpx`，
  无边框无阴影，靠间距与底色分层。
- 上述设计语言是现行实现，不是不可更改的约束；本次重构可重新评估。

## Evidence on Hand

- `src/data/templates.ts` 的 `USER_TEMPLATES`：11 条真实订阅记录，含金额、币种、
  周期、扣费日、自动续费状态。这是唯一的一手使用数据。
- `src/data/templates.ts` 的 `TEMPLATES`：28 条通用订阅模板。

**不存在、未来工作不得虚构**：用户评价、案例、媒体报道、付费数据、竞品对比、
用户量、增长率、定价方案。

## Product Principles

1. **扣费时点优先于一切**——任何界面元素让位给「什么时候扣、扣多少」。
2. **试用不污染正式统计**——免费的东西不能被算成支出。
3. **手动录入是特性，不是缺陷**——不接受用隐私换取便利。
4. **跨币种必须可比**——不同币种不能因为单位不同就失去可比性。
5. **提醒只在需要决策时出现**——不做装饰性通知，避免用户对提醒脱敏。
